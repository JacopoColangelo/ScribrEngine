---
description: Start the ScribrEngine development server
---
# Start Dev Server

This workflow starts the development server. Since `vite.config.js` is configured with `open: true`, this will automatically open your default browser to the game.

// turbo
1. Start the server
```bash
node node_modules/vite/bin/vite.js
```
